// Nick Arboscello
/**
 * The RegisteredUser class represents a registered user in the system.
 */
public class RegisteredUser {
    private String userName;    // The username of the registered user.
    private String password;    // The password of the registered user.
    private String firstName;   // The first name of the registered user.
    private String lastName;    // The last name of the registered user.
    private String email;       // The email address of the registered user.
    private int privacyLevel;   // The privacy level of the registered user.

    /**
     * Constructs a new RegisteredUser object.
     */
    public RegisteredUser() {
        // Constructor for RegisteredUser.
        // Initialize member variables as needed.
        
    }

    /**
     * Adds a main project for the registered user.
     */
    public void addMainProject() {
        // Implementation details for adding a main project.
        // Replace with actual logic.
        
    }

    /**
     * Adds a project manager role for the registered user.
     */
    public void addProjectManager() {
        // Implementation details for adding a project manager role.
        // Replace with actual logic.
        
    }

    /**
     * Gets the username of the registered user.
     *
     * @return The username of the registered user.
     */
    public String getUsername() {
        return userName;
    }
}

